export const SCHEDULER_CLASSES = {
  grid: {
    container: "flex flex-1 overflow-hidden",
    leftColumn: "flex-shrink-0 border-r bg-white",
    content: "flex-1 overflow-auto",
    header: "border-b"
  }
} as const;
